## 2025-05-15 - Header Accessibility and Interaction
**Learning:** Adding standard ARIA roles (`menu`, `menuitem`) and handling the `Escape` key and clicks outside significantly improves navigation accessibility and user intuition with minimal code overhead.
**Action:** Always implement `aria-expanded`, `aria-haspopup`, and `Escape` key listeners for interactive navigation dropdowns and mobile menus.
