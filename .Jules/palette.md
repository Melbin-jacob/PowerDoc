## 2025-05-14 - Accessible Navigation Menus
**Learning:** For accessible navigation, combining `useId` for stable ARIA IDs with `Escape` key listeners and `mousedown` for click-outside handlers ensures a robust experience for both keyboard and mouse users.
**Action:** Always implement `aria-expanded`, `aria-controls`, and keyboard listeners for interactive dropdowns.
